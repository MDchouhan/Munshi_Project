
import androidx.compose.ui.graphics.Color
import androidx.compose.ui.text.TextStyle
import androidx.compose.ui.text.font.FontFamily
import androidx.compose.ui.unit.sp

val LeagueSpartan = FontFamily.Default // Replace with your custom font if available

val newAccountTextStyle = TextStyle(
    fontSize = 24.sp,
    color = Color(0xFF2260FF),
    fontFamily = LeagueSpartan
)

val fullNameTextStyle = TextStyle(
    fontSize = 20.sp,
    color = Color.Black,
    fontFamily = LeagueSpartan
)

val passwordTextStyle = TextStyle(
    fontSize = 20.sp,
    color = Color.Black,
    fontFamily = LeagueSpartan
)

val exampleTextStyle = TextStyle(
    fontSize = 20.sp,
    color = Color(0xFF809CFF),
    fontFamily = LeagueSpartan
)

val byContinuingTextStyle = TextStyle(
    fontSize = 12.sp,
    color = Color(0xFF070707),
    fontFamily = LeagueSpartan
)

val dateOfBirthTextStyle = TextStyle(
    fontSize = 20.sp,
    color = Color.Black,
    fontFamily = LeagueSpartan
)

val ddMmYyyTextStyle = TextStyle(
    fontSize = 20.sp,
    color = Color(0xFF2260FF),
    fontFamily = LeagueSpartan
)